snake-classic-standalone

Install
1) npm install
2) npm run dev
3) Open the served URL that esbuild prints

Notes
- This repo is standalone by design, no shared core.
- Replace placeholder visuals with your own sprites and tiles.
- To wrap as a mobile app later, use Capacitor, npx cap add android, npx cap add ios.
